## Paso 1 - Hacer el build de Docker
Situarnos en la raiz del proyecto, donde se encuentra el archivo **docker-compose.yml** y ejecutamos:<br>
<code>docker-compose build</code><br>
<code>docker-compose up</code>
